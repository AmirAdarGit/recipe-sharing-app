{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "npm run docker:start && nodemon src/index.js",
    "start": "node src/index.js",
    "docker:start": "docker run -d --name recipe-sharing-app-local -p 27017:27017 mongo:latest || docker start recipe-sharing-app-local",
    "docker:stop": "docker stop recipe-sharing-app-local",
    "docker:clean": "docker stop recipe-sharing-app-local && docker rm recipe-sharing-app-local",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "firebase-admin": "^13.4.0",
    "mongoose": "^8.17.0"
  },
  "devDependencies": {
    "@types/node": "^24.1.0",
    "concurrently": "^9.2.0",
    "cross-env": "^10.0.0",
    "eslint": "^9.32.0",
    "nodemon": "^3.1.10",
    "prettier": "^3.6.2",
    "rimraf": "^6.0.1"
  }
}
